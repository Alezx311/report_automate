## 📊 Статистика після парсингу

Після обробки PST додаток показує:

- **Всього threads**: кількість унікальних переписок
- **Всього звернень**: загальна кількість створених звернень
- **Вирішено**: звернення з відповіддю від техпідтримки
- **У процесі**: звернення без відповіді

Це дозволяє швидко оцінити обсяг роботи та стан звернень перед експортом.# PST Email Parser - Додаток для парсингу звернень

Десктопний додаток для автоматизованого парсингу PST-файлів Outlook/Exchange з генерацією CSV звіту про звернення в техпідтримку.

## 🚀 Особливості

- ✅ Парсинг локальних PST-файлів без необхідності підключення до Exchange
- ✅ Автоматичне групування повідомлень в threads (переписки)
- ✅ Інтелектуальна логіка розбиття на окремі звернення:
  - Кожне повідомлення клієнта = нове звернення
  - Відповідь техпідтримки закриває звернення
  - Автоматичне закриття при повторному зверненні клієнта
- ✅ Генерація чорнового CSV звіту для редагування в Excel
- ✅ Прев'ю перших 500 рядків в додатку
- ✅ Пошук ключових слів та фільтрація
- ✅ Визначення важливості звернення
- ✅ Крос-платформенність (Windows, macOS, Linux)

## 📋 Вимоги

- Node.js 18+ ([завантажити](https://nodejs.org/))
- npm (встановлюється разом з Node.js)

## 🔧 Встановлення

1. **Розпакуйте архів** з усіма файлами проєкту

2. **Встановіть залежності**:
```bash
npm install
```

## ▶️ Запуск

### Режим розробки:
```bash
npm start
```

### Збірка виконуваного файлу:
```bash
npm run build
```

Готовий додаток буде у папці `dist/`

## 📖 Використання

1. **Запустіть додаток**
2. **Натисніть "Оберіть PST-файл"** та виберіть експортований файл Outlook
3. **Вкажіть email техпідтримки** (через кому), наприклад:
   ```
   OSHB.ESB.Support@service-team.biz, Oleksiy_Sokolov@service-team.biz
   ```
4. **Додайте ключові слова** для пошуку (опціонально):
   ```
   пришліть логи, фіксуємо помилки, перезапуск, терміново
   ```
5. **Вкажіть фільтр** (опціонально) - слово, яке має бути в темі або тексті
6. **Натисніть "Парсити та генерувати CSV"**
7. **Зачекайте** на завершення обробки
8. **Знайдіть CSV файл** на робочому столі: `report_draft_YYYY-MM-DD.csv`

## 📊 Структура CSV звіту

Згенерований файл містить колонки:

| Колонка | Опис |
|---------|------|
| Дата реєстрації проблеми | YYYY-MM-DD |
| Час реєстрації проблеми | HH:MM:SS |
| Система | Автоматично витягнута (або порожнє) |
| ID заявки у SD | Порожнє (для ручного заповнення) |
| Комунікація | Завжди "Пошта" |
| Опис проблеми | Перші 300 символів + знайдені ключові слова |
| Статус | Вирішено / Вирішено частково / Не закрито |
| Відповідальний | Ім'я співробітника техпідтримки |
| Рішення | Перші 200 символів відповіді |
| Дата вирішення проблеми | YYYY-MM-DD |
| Час вирішення проблеми | HH:MM:SS |
| Критерій важливості | Високий / Низький |

## 🔍 Логіка обробки

### Створення звернень з threads

1. **Thread (переписка)** = група повідомлень з однаковим `conversationId` або `subject`
2. **Перший лист у thread** = завжди початок нового звернення
3. **Обробка наступних листів:**
   - Якщо лист **НЕ від техпідтримки** → додається до опису поточного звернення
   - Якщо лист **від техпідтримки** → закриває поточне звернення (статус "Вирішено")
4. **Після закриття звернення:**
   - Якщо є ще листи від клієнта → створюється нове звернення
   - Кожна відповідь техпідтримки = окреме закрите звернення

### Структура опису звернення

Опис містить детальні блоки:

```
=== ТЕМАТИКА ===
Тема: [тема листа]
Відповідальний: [ім'я з техпідтримки]
Час відповіді: [розрахований час між зверненням і відповіддю]

=== СТАТИСТИКА ===
Всього листів: X
Листів від клієнта: X
Листів від техпідтримки: X
Учасники: [список всіх учасників]

=== ПЕРЕПИСКА ===
[Дата Час] Ім'я:
Нормалізований текст повідомлення...
---
[наступні повідомлення]
```

### Визначення статусу

- **Вирішено**: є відповідь від техпідтримки
- **У процесі**: немає відповіді від техпідтримки

### Визначення важливості

- **Високий**: у тексті або темі є слово "прод"
- **Низький**: у тексті або темі є слово "тест"
- **Середній**: всі інші випадки

### Розрахунок часу відповіді

Автоматично розраховується час між останнім листом клієнта та відповіддю техпідтримки:
- До 24 годин: "X год Y хв"
- Більше 24 годин: "X днів Y год"

## ⚠️ Обмеження

- Максимальний розмір PST: **2GB** (якщо більше - розділіть файл в Outlook)
- Підтримуються тільки локальні PST файли
- Немає інтеграції з Graph API або Exchange Online

## 🐛 Вирішення проблем

### Помилка "Файл завеликий"
- Розділіть PST на менші частини в Outlook
- Експортуйте окремі папки замість усього поштового ящика

### Помилка "Немає даних"
- Перевірте, що PST містить повідомлення
- Перевірте фільтри - можливо, вони занадто обмежувальні

### Додаток не запускається
```bash
# Видаліть node_modules та переустановіть
rm -rf node_modules
npm install
npm start
```

## 📁 Структура проєкту

```
pst-email-parser/
├── package.json          # Конфігурація проєкту
├── main.js              # Головний процес Electron + логіка парсингу
├── preload.js           # IPC bridge
├── renderer.js          # UI логіка
├── index.html           # Інтерфейс додатка
├── styles.css           # Стилі
└── README.md            # Ця документація
```

## 🔄 Оновлення

Для оновлення бібліотек:
```bash
npm update
```

## 📝 Ліцензія

MIT License - вільне використання

## 💡 Підтримка

При виникненні проблем:
1. Перевірте консоль розробника (розкоментуйте `openDevTools()` в `main.js`)
2. Перевірте логи в терміналі
3. Переконайтеся, що PST файл не пошкоджений

---

**Версія**: 1.0.0  
**Дата**: 2024  
**Розробник**: Your Name